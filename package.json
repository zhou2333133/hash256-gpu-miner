{
  "name": "hash256-gpu-miner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/index.js",
    "dev": "tsx src/index.ts",
    "selftest": "tsx src/selftest.ts",
    "submit-nonce": "tsx src/submitNonce.ts",
    "check": "tsc --noEmit -p tsconfig.json",
    "build:cuda": "node scripts/build-cuda.mjs",
    "build:cuda:win": "node scripts/build-cuda.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
